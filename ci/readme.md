exec `sh ./install.sh` to deploy service.

Envs:
  - `BPFAAS_CI_VAULT_ADDR`: vault addr.
  - `BPFAAS_CI_VAULT_TOKEN`: vault token.

Files:
  - `~/.ssh/bpfaas/bpfaas-lib-bps-web-spring-cloud-starter`: git private key.